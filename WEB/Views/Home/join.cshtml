<!DOCTYPE html>
<html lang="cs">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Připoj se - Habibi Meet</title>
    <link rel="stylesheet" href="styles.css">
</head>
<body>
    <header>
        <nav>
            <div class="logo">Habibi Meet</div>
            <button class="mobile-menu-btn" onclick="toggleMobileMenu()">☰</button>
            <ul class="nav-links" id="navLinks">
                <li><a href="index.html">Domů</a></li>
                <li><a href="features.html">Funkce</a></li>
                <li><a href="about.html">O nás</a></li>
                <li><a href="join.html">Připoj se</a></li>
            </ul>
        </nav>
    </header>

    <main>
        <section class="join-section">
            <div class="join-container">
                <h1>Začni svou cestu</h1>
                <p class="join-subtitle">Vytvoř si účet a staň se součástí Habibi Meet</p>

                <form class="signup-form" id="signupForm" onsubmit="handleSubmit(event)">
                    <div class="form-group">
                        <label>Celé jméno</label>
                        <input type="text" id="fullName" placeholder="Jan Novák" required>
                        <span class="error-message" id="nameError"></span>
                    </div>

                    <div class="form-group">
                        <label>E-mail</label>
                        <input type="email" id="email" placeholder="tvuj@email.cz" required>
                        <span class="error-message" id="emailError"></span>
                    </div>

                    <div class="form-group">
                        <label>Uživatelské jméno</label>
                        <input type="text" id="username" placeholder="@tvojeuzivatelskejmeno" required>
                        <span class="error-message" id="usernameError"></span>
                    </div>

                    <div class="form-group">
                        <label>Heslo</label>
                        <input type="password" id="password" placeholder="••••••••" required>
                        <div class="password-strength" id="passwordStrength"></div>
                        <span class="error-message" id="passwordError"></span>
                    </div>

                    <button type="submit" class="cta-button full-width">Vytvořit účet</button>

                    <p class="terms">Registrací souhlasíš s našimi podmínkami použití a zásadami ochrany osobních údajů</p>
                </form>

                <div class="login-option">
                    <p>Už máš účet? <a href="#">Přihlaš se zde</a></p>
                </div>

                <div class="success-message" id="successMessage">
                    <span class="success-icon">✓</span>
                    <p>Účet byl úspěšně vytvořen! Vítej v Habibi Meet 🎉</p>
                </div>
            </div>

            <div class="join-benefits">
                <h2>Co získáš</h2>
                <div class="benefits-list">
                    <div class="benefit">
                        <span class="benefit-icon">✓</span>
                        <div>
                            <h3>Bezplatný účet navždy</h3>
                            <p>Všechny základní funkce zdarma, žádné skryté poplatky</p>
                        </div>
                    </div>
                    <div class="benefit">
                        <span class="benefit-icon">✓</span>
                        <div>
                            <h3>Okamžitý přístup</h3>
                            <p>Začni sdílet a komunikovat ihned po registraci</p>
                        </div>
                    </div>
                    <div class="benefit">
                        <span class="benefit-icon">✓</span>
                        <div>
                            <h3>Bezpečnost dat</h3>
                            <p>Tvoje informace jsou chráněny špičkovým zabezpečením</p>
                        </div>
                    </div>
                    <div class="benefit">
                        <span class="benefit-icon">✓</span>
                        <div>
                            <h3>Žádné reklamy</h3>
                            <p>Užívej si síť bez obtěžujících reklam</p>
                        </div>
                    </div>
                </div>
            </div>
        </section>
    </main>

    <footer>
        <p>&copy; 2024 Habibi Meet. Všechna práva vyhrazena.</p>
        <p>Vytvořeno jako školní projekt</p>
    </footer>

    <button class="scroll-to-top" id="scrollToTop" onclick="scrollToTop()">↑</button>

    <script>
        // Mobile menu toggle
        function toggleMobileMenu() {
            const navLinks = document.getElementById('navLinks');
            navLinks.classList.toggle('mobile-active');
        }

        // Scroll to top button
        const scrollToTopBtn = document.getElementById('scrollToTop');
        
        window.addEventListener('scroll', () => {
            if (window.pageYOffset > 300) {
                scrollToTopBtn.classList.add('visible');
            } else {
                scrollToTopBtn.classList.remove('visible');
            }
        });

        function scrollToTop() {
            window.scrollTo({
                top: 0,
                behavior: 'smooth'
            });
        }

        // Validace hesla v reálném čase
        const passwordInput = document.getElementById('password');
        const strengthIndicator = document.getElementById('passwordStrength');

        passwordInput.addEventListener('input', (e) => {
            const password = e.target.value;
            let strength = 0;
            let strengthText = '';
            let strengthColor = '';

            if (password.length >= 8) strength++;
            if (password.match(/[a-z]/) && password.match(/[A-Z]/)) strength++;
            if (password.match(/[0-9]/)) strength++;
            if (password.match(/[^a-zA-Z0-9]/)) strength++;

            switch(strength) {
                case 0:
                case 1:
                    strengthText = 'Slabé heslo';
                    strengthColor = '#ef4444';
                    break;
                case 2:
                    strengthText = 'Průměrné heslo';
                    strengthColor = '#f59e0b';
                    break;
                case 3:
                    strengthText = 'Dobré heslo';
                    strengthColor = '#10b981';
                    break;
                case 4:
                    strengthText = 'Výborné heslo';
                    strengthColor = '#059669';
                    break;
            }

            if (password.length > 0) {
                strengthIndicator.textContent = strengthText;
                strengthIndicator.style.color = strengthColor;
                strengthIndicator.style.display = 'block';
            } else {
                strengthIndicator.style.display = 'none';
            }
        });

        // Validace formuláře
        function validateForm() {
            let isValid = true;
            
            const fullName = document.getElementById('fullName').value.trim();
            const email = document.getElementById('email').value.trim();
            const username = document.getElementById('username').value.trim();
            const password = document.getElementById('password').value;

            // Reset error messages
            document.querySelectorAll('.error-message').forEach(el => el.textContent = '');

            // Validace jména
            if (fullName.length < 2) {
                document.getElementById('nameError').textContent = 'Jméno musí mít alespoň 2 znaky';
                isValid = false;
            }

            // Validace emailu
            const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
            if (!emailRegex.test(email)) {
                document.getElementById('emailError').textContent = 'Zadej platný email';
                isValid = false;
            }

            // Validace uživatelského jména
            if (username.length < 3) {
                document.getElementById('usernameError').textContent = 'Uživatelské jméno musí mít alespoň 3 znaky';
                isValid = false;
            }

            // Validace hesla
            if (password.length < 8) {
                document.getElementById('passwordError').textContent = 'Heslo musí mít alespoň 8 znaků';
                isValid = false;
            }

            return isValid;
        }

        // Zpracování odeslání formuláře
        function handleSubmit(event) {
            event.preventDefault();
            
            if (validateForm()) {
                // Simulace úspěšné registrace
                document.getElementById('signupForm').style.display = 'none';
                document.querySelector('.login-option').style.display = 'none';
                document.getElementById('successMessage').style.display = 'block';
                
                // V reálné aplikaci by zde bylo API volání
                console.log('Formulář odeslán');
            }
        }

        // Animace při načtení
        const animateOnScroll = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.style.opacity = '1';
                    entry.target.style.transform = 'translateY(0)';
                }
            });
        }, { threshold: 0.1 });

        document.querySelectorAll('.join-container, .join-benefits, .benefit').forEach((el, index) => {
            el.style.opacity = '0';
            el.style.transform = 'translateY(30px)';
            el.style.transition = 'opacity 0.6s ease, transform 0.6s ease';
            el.style.transitionDelay = `${index * 0.1}s`;
            animateOnScroll.observe(el);
        });

        // Focus efekty na inputy
        document.querySelectorAll('.form-group input').forEach(input => {
            input.addEventListener('focus', function() {
                this.parentElement.classList.add('focused');
            });
            
            input.addEventListener('blur', function() {
                if (this.value === '') {
                    this.parentElement.classList.remove('focused');
                }
            });
        });
    </script>
</body>
</html>